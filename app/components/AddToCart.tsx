"use client";

import React from "react";

const AddToCart = () => {
    return <button className="btn btn-primary" onClick={() => console.log("click")}>AddToCart</button>;
};

export default AddToCart;
